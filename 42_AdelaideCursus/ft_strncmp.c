/*strncmp compares at most the first n bytes of str1 ans str2*/
#include <stddef.h>

int ft_strncmp(const char *str1, const char *str2, size_t n)
{
    int i;

    i = 0;
    while ((str1[i] != '\0') && (i <= n))
    {
        
    }

}